# textcanvas

A simple starter template for a Python library.

## Structure

- `src/textcanvas/` contains the package source code
- `tests/` contains basic tests
- `pyproject.toml` defines packaging and dependencies

## Development

Create and activate a virtual environment:

```bash
python -m venv .venv
source .venv/bin/activate
```

Install the package in editable mode:

```bash
pip install -e .
```

Run tests:

```bash
python -m unittest discover -s tests -v
```

## Next steps

- Replace the example module with your own code
- Add more tests for real behavior
- Publish to PyPI when ready
